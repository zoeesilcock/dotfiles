windows:
  - name: "mnd-services"
    root: "~/Projects/mynewsdesk"
    layout: main-vertical
    panes:
      - cmd:
        - "bundle exec rake jobs:work"
        - "bundle exec rake jobs:clear"
      - cmd: "solr"
      - cmd: "solr test"
